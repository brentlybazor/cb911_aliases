# cd ~

# check the OS 

unameOut="$(uname -s)"

# OS is Mac
if [[ "$unameOut" == *"Darwin"* ]]; then
    myDir=$(dirname $0)
fi

# OS is Windows
if [[ "$unameOut" == *"MING"* ]]; then
    myDir="${BASH_SOURCE%/*}"
fi

source $myDir/drm_aliases.sh
source $myDir/docker_aliases.sh
